# **AlphaMind 系统架构设计文档（V2.2）**

## **文档信息**

**项目**

**内容**

文档名称

AlphaMind 系统架构设计文档

版本号

V2.2

编制日期

2026-04-25

修订说明

1\. 明确并发槽位调度中的饥饿防止策略；2. 明确批量插队仅验证不修正；3. 统一高优/普通任务切换阈值；4. 增加多优先级细分功能的测试；5. 统一术语“模拟槽位”；6. 补充 RESTful API 端点定义

适用对象

产品经理、开发工程师、测试工程师、运维工程师

## **一、产品定义与定位**

### **1.1 产品定义**

AlphaMind 是一个面向 WorldQuant Brain 平台的 AI 驱动 Alpha 因子全流程自动化研究系统。系统深度融合大语言模型（LLM）技术与传统量化研究方法，提供从因子生成、多策略优化、自动化模拟测试、智能筛选、自相关检测到平台提交的完整闭环。同时，系统具备全流程数据持久化存储和 Web 端实时可视化监控能力，旨在显著提升 Alpha 因子研究的效率和质量。

### **1.2 目标用户**

- 在 WorldQuant Brain 平台上进行因子研究的量化分析师
- 希望利用 AI 加速 Alpha 发现过程的量化团队
- 需要自动化、可监控的因子挖掘流水线的机构用户
- 需要灵活进行批量模拟实验的研究人员

### **1.3 核心价值**

- **智能化**：借助本地部署的大语言模型自动生成高质量的 Alpha 表达式，降低对人工经验的依赖。
- **自动化**：实现从研究想法到平台提交的全自动化链路，支持 7×24 小时无人值守运行。
- **可进化**：内置遗传演化引擎和自优化机制，使因子能够持续迭代改进。
- **高可靠**：多层次筛选、自相关去重和质量监控确保输出高质量因子。
- **可观测**：Web Dashboard 提供实时的系统状态、挖掘进度和因子质量可视化，便于管理与决策。
- **灵活插队**：支持用户主动发起批量模拟任务，将指定表达式优先或按策略插入执行队列，保证研究灵活性。

## **二、功能模块与具体描述**

系统由以下核心功能模块组成，各模块协同工作形成完整的 Alpha 研究流水线。

### **2.1 平台认证与会话管理模块**

负责与 WorldQuant Brain 平台的安全通信。通过加载用户凭证实现自动认证，并维护会话的长期有效性。在检测到认证过期时自动重新认证，确保所有 API 操作的连续性和可靠性。

### **2.2 LLM 服务模块**

作为 AI 驱动因子生成的核心，统一管理本地大语言模型（通过 Ollama）和云端模型（如 DeepSeek API）。该模块负责根据上下文提示生成 Alpha 表达式，并具备以下关键特性：

- 本地优先的生成策略，在本地服务不可用时自动降级到云端 API。
- 请求速率限制，保障 API 调用的稳定性和合规性。
- 生成结果的初步清洗与格式校验。

### **2.3 模板生成与验证模块**

解析用户的研究主题或挖掘方向，结合平台可用的操作符与数据字段，构建面向 LLM 的结构化提示，生成符合 FASTEXPR 语法的 Alpha 表达式。对所生成的表达式执行语法、语义验证和自动修正流程，确保最终表达式在提交模拟前是合法且有效的。

### **2.4 模拟测试模块**

将经过验证的 Alpha 表达式批量提交至 WorldQuant Brain 平台进行模拟测试。该模块异步管理多个并发任务，持续监控每个任务的状态，并在完成后自动抓取结果（如夏普比率、收益率、最大回撤等关键绩效指标）。

### **2.5 批量插队模拟模块**

该模块为用户提供灵活的研究工具，允许将一批 Alpha 表达式以插队方式提交模拟，而不必等待常规调度队列。支持两种提交途径，并提供完整的管理与反馈能力。

**具体功能：**

- **双模式提交**：
  - **CSV 文件上传**：用户可上传包含 Alpha 表达式和相关参数的 CSV 文件，系统批量解析并提交。
  - **系统内选择**：从已有的表达式库、生成历史或因子库中选取表达式，或手动输入多个表达式，打包提交。
- **插队机制**：提交的任务被置入高优先级队列，在并发槽位空闲时优先于普通生成任务执行，但不抢占正在执行的任务。支持用户指定优先级等级（普通、高、紧急），同优先级内按提交时间顺序执行。
- **表达式格式验证**：提交前仅对每一条表达式进行语法和字段合规性校验（不触发自动修正），确保为有效 FASTEXPR 语句。错误表达式直接拒绝并反馈原因。
- **批量进度展示**：在 Web 界面实时更新整体进度（已完成数/总数）、各表达式状态（排队中、进行中、已完成、失败）。
- **状态反馈与错误详情**：每一条表达式插队后，清晰显示其状态；失败的提供具体错误信息（语法错、字段不存在、模拟超时等）。
- **结果导出**：支持将本次批量任务的最终结果（含表达式、区域、夏普比率、回撤等各项指标）导出为 CSV 文件，便于本地分析。
- **任务优先级管理界面**：在 Web Dashboard 提供独立的批量任务管理视图，可查看所有活跃及历史批量任务，支持调整未执行任务的优先级或取消任务。

### **2.6 因子筛选与自相关检测模块**

对批量模拟测试的结果进行自动化分析，依据预设的质量阈值（如夏普比率下限）初步筛选合格因子。对合格因子集合执行自相关去重分析，剔除与已有优质因子相关性过高的重复变体，确保因子库的多样性和低冗余度。

### **2.7 演化优化模块**

基于遗传算法等进化计算技术，对现有合格因子进行迭代优化。核心操作包括：

- **选择**：从因子种群中挑选高适应度个体作为父本。
- **交叉**：组合不同父本的表达式结构，生成新的组合因子。
- **变异**：对因子的操作符、参数或数据结构进行随机微调。
- **精英保留**：将每一代中最优的因子直接保留至下一代，防止优质基因丢失。

此模块还集成了自优化器，根据演化表现动态调整变异率、交叉率等关键参数。

### **2.8 质量监控与退化检测模块**

持续追踪已入库和已提交的 Alpha 因子在后续时段的表现，计算其稳定性、趋势和综合健康评分。当检测到某个因子的关键指标（如夏普比率）相比历史平均水平出现显著下滑时，自动发出告警并建议重新生成或淘汰。

### **2.9 持久化存储模块**

为全流程提供结构化数据存储。主要存储内容包括：所有生成表达式的历史记录、每次模拟测试的详细结果、演化迭代过程中的种群快照、因子库档案以及系统配置与审计日志。存储方案采用轻量级关系型数据库，支持高效的查询检索和备份恢复。

### **2.10 挖掘协调调度模块**

作为系统的核心调度中心，协调各模块的有序运行。管理每日模拟测试额度、控制并发任务数量、实现跨区域和多主题的轮巡挖掘策略，并支持连续不间断的无人值守运行。负责维护包含常规自动任务和用户插队任务的多级优先级队列。

### **2.11 Web Dashboard 可视化模块**

提供基于浏览器的用户界面，支持以下核心功能：

- 系统运行状态总览（如 LLM 服务状态、模拟任务进度、今日已用额度）。
- 挖掘任务的启动、暂停、取消与参数配置。
- 批量插队模拟任务管理：上传 CSV、选择表达式、启动插队、查看进度、导出结果。
- 因子库的浏览、搜索、筛选与详情查看（含绩效图表和演化历史）。
- 系统日志的实时查看与过滤。

## **三、技术实现方法与路径**

本节详细说明各功能模块的技术选型、实现路径和关键步骤。

### **3.1 平台认证与会话管理模块**

**技术选型**：基于 `requests` 库的 `Session` 对象实现 HTTP 通信持久化，配合 WorldQuant Brain SDK（wqb）进行高层封装。

**实现路径**：

1. 读取本地凭证文件（JSON 格式），解析出用户名和密码。
2. 以 `HTTPBasicAuth` 方式初始化 HTTP 会话，并调用 WorldQuant Brain 认证接口完成登录。
3. 将会话实例注入至所有需要 API 调用的模块中。
4. 实现自动重认证中间件：在每个 API 请求返回 401 状态码时，自动触发重新认证流程并重试原请求，重试次数上限为 3 次，采用指数退避等待。

### **3.2 LLM 服务模块**

**技术选型**：本地采用 Ollama 框架运行量化微调模型（如 `qwen2.5-coder:1.5b`），云端降级采用 DeepSeek API。通过统一的接口抽象屏蔽不同后端的差异。

**实现路径**：

1. 部署 Ollama 服务并拉取所需的模型文件。
2. 封装 Ollama 客户端，通过 `http://localhost:11434` 的 `/api/generate` 接口进行生成。
3. 封装 DeepSeek 客户端，使用 `chat/completions` 兼容接口。
4. 实现健康检测机制：定期向 Ollama 服务发送心跳请求，连续失败超过阈值时切换为“降级”状态。
5. 实现统一生成入口：首先尝试本地 Ollama，失败时自动调用 DeepSeek，若仍失败则返回空并向日志报警。
6. 实现请求限流：采用线程本地变量记录每个线程的最近请求时间，确保两次请求间隔不低于设定的最小间隔（如 2 秒）。

### **3.3 模板生成与验证模块**

**技术选型**：提示词工程 + AST（抽象语法树）解析 + 自修正规则引擎。

**实现路径**：

1. **提示词构建**：每次生成时，动态获取指定区域支持的操作符列表和数据字段列表，构建包含操作符说明、字段描述、过去已生成表达式的去重上下文和错误避免提示的结构化提示。
2. **表达式生成**：调用 LLM 服务生成原始表达式文本。
3. **语法验证**：使用词法分析器将表达式分解为 Token 序列，然后利用递归下降解析器构建 AST，在此过程中检测括号不匹配、非法操作符、非法字段等语法错误。
4. **语义验证**：在 AST 基础上，检查操作符与输入参数在数量和类型上的兼容性（如矩阵操作符不能使用标量输入）。
5. **自动修正**：对于验证失败的表达式，通过分析错误特征（如“事件输入不兼容”、“参数数量错误”），匹配预定义的修正规则库或从历史成功修正案例中学习，尝试进行自动修复。若修正失败，则将错误信息反馈至 LLM 进行二次生成。

### **3.4 模拟测试模块**

**技术选型**：多线程并发 + 异步轮询状态管理。

**实现路径**：

1. 根据区域配置（区域、股票池、延迟参数等）构造模拟提交参数。
2. 使用 `ThreadPoolExecutor` 创建线程池（最大并发数 8），将待测试因子分发至线程池进行提交。
3. 每个线程在提交后获得进度 URL，进入状态轮询循环：每隔 5 秒查询一次任务状态，直到状态变为“COMPLETE”或“FAILED”，或总等待时间超过 300 秒。
4. 任务完成后，通过 Alpha ID 获取详细的回测摘要数据（夏普、适应度、换手率、收益、最大回撤等），并将其封装为标准的结果数据结构。
5. 所有结果通过回调或队列汇聚至筛选模块。

### **3.5 批量插队模拟技术实现**

**技术选型**：多优先级任务队列 + CSV 解析 + 前端文件上传与交互。

**实现路径**：

1. **双优先级队列设计**：在挖掘协调调度器中构建两个优先级队列：普通队列（自动生成任务）和高优先级队列（用户插队任务）。高优先级队列内部支持细分优先级编号（数值越小优先级越高），同优先级按提交时间先进先出。总并发槽位数为8，其中7个槽位可被高/普通任务动态抢占，1个保留槽位仅执行普通队列任务（当普通队列为空时可降级执行高优先级任务），以防止普通任务长期饥饿。
2. **CSV 上传解析**：前端提供文件上传组件，CSV 文件必需列包括“expression”，可选列包括“region”、“universe”、“delay”。后端接收文件后逐行解析并去除空白行。
3. **系统内选择提交**：前端提供因子库浏览界面，支持多选框和搜索功能，用户可勾选多个因子表达式加入插队列表；同时提供文本框供用户手动输入多行表达式。
4. **提交前验证**：对所有拟插队的表达式逐一调用模板生成与验证模块中的语法和语义验证器，**仅执行验证不触发自动修正**。验证通过的进入待插队列表；验证失败的将错误信息直接返回前端，并拒绝加入队列。
5. **任务创建与入队**：为一批插队表达式生成一个批量任务 ID（Batch ID），并将每一条表达式以独立任务项加入高优先级队列，关联该 Batch ID。
6. **进度展示与状态更新**：后端在每个独立任务完成时，更新数据库中对应 Batch 的进度计数，并通过 WebSocket 向相关客户端推送批量任务的实时进度。前端在批量任务详情页实时刷新每一条表达式的状态。
7. **结果导出**：批量任务全部终结后，前端提供“导出 CSV”按钮。后端查询该 Batch 下所有任务结果，组装为 CSV 文件供下载，文件包含表达式、区域、夏普比率、适应度、换手率、状态等字段。
8. **不影响现有任务**：插入的高优任务不会中断正在执行的普通任务。调度器在单个槽位上连续执行高优先级任务数量达到可配置阈值（默认4个）后，强制切换到普通队列取一个任务执行，以确保调度公平。

### **3.6 因子筛选与自相关检测模块**

**技术选型**：延迟贪心算法 + 表达式相似度计算。

**实现路径**：

1. **初级筛选**：设定夏普比率最低阈值（如 1.25），仅保留达标的结果。
2. **自相关去重**：
   - 计算所有达标因子之间的两两相似度，相似度度量基于表达式结构特征（操作符集合、字段使用、复杂度）的加权相似性，并结合 WorldQuant 平台返回的因子间原始相关性数据。
   - 采用贪心策略按夏普比率降序挑选因子：每挑选一个因子，就将其余与其相似度超过设定阈值（如 0.85）的因子从候选池中移除，直至候选池为空。
3. 输出最终去重后的合格因子列表。

### **3.7 演化优化模块**

**技术选型**：标准遗传算法框架 + 自适应参数调整机制。

**实现路径**：

1. **种群初始化**：以当前合格因子作为初始种群，若数量不足则引入随机变异填充至预设规模（如 50）。
2. **适应度计算**：适应度函数为：`0.5 * Sharpe + 0.3 * Fitness - 0.2 * Turnover`。对于未能获得完整模拟结果的个体，赋予最低适应度。
3. **父本选择**：采用锦标赛选择，每次随机抽取 3 个个体，适应度最高者进入交配池。
4. **交叉操作**：对选中的两个父本，解析其表达式树，随机交换一棵子树（如时间序列子表达式），生成两个子代。
5. **变异操作**：对子代按预设概率（初始 0.1）应用变异，随机选择替换操作符、修改数值参数（如窗口长度）或增减包装函数等策略。
6. **精英保留与新一代形成**：将本代适应度最高的前 10% 个体直接复制至新一代，其余由交叉变异产生的子代补足，保持种群规模不变。
7. **自适应参数调整**：每进行 100 次模拟测试后，评估近期成功率。若成功率低于 30%，则增大变异率和探索程度；若高于 60%，则降低变异率并提高交叉率，以提升收敛速度。

### **3.8 质量监控与退化检测模块**

**技术选型**：滑动窗口统计 + 线性趋势分析。

**实现路径**：

1. 为每个因子维护一个长度为 30 天的性能记录滑动窗口。
2. 每日将当天的因子绩效指标（夏普、收益等）追加至窗口中，并清理超期数据。
3. **退化检测**：当窗口内数据点超过 10 个时，比较最近 10 次的平均夏普与前 20 次（或更早批次）的平均夏普，若降幅超过 20%，则判定为退化。
4. **健康评分**：综合“稳定性”（夏普数值标准差的倒数）和“趋势”（夏普序列的线性拟合斜率）计算 0 到 1 的健康分。稳定性权重 0.6，趋势权重 0.4。趋势为负时该项为零。
5. 健康评分低于 0.4 或检测到退化的因子，将被标记并推送告警，建议从因子库中剔除或重新演化。

### **3.9 持久化存储模块**

**技术选型**：SQLite 数据库 + 文件系统缓存。

**实现路径**：

1. 设计标准化的数据库表结构，至少包含：因子库主表、模拟结果明细表、演化代际记录表、编译器知识表、批量插队任务表。
2. 在每次模拟完成后，将完整结果写入模拟结果明细表，并将通过筛选的因子同步更新至因子库主表。
3. 实现因子计算结果的文件缓存：以因子表达式的 MD5 哈希为键，将计算出的因子值或中间结果存储为 PKL 或 HDF5 文件，加速后续回测验证。
4. 数据库操作封装为专用的数据访问层，支持批量写入、事务控制以及按多种条件组合查询。
5. 提供数据库备份接口，支持定时自动备份与手动恢复。

### **3.10 挖掘协调调度模块**

**技术选型**：状态机 + 多优先级队列管理 + 槽位并发控制。

**实现路径**：

1. 定义挖掘任务的状态机，包含状态：启动、生成中、模拟中、演化中、暂停、停止、错误。
2. 初始化模板生成队列和模拟任务队列，包含普通优先级和高优先级两个逻辑分区。高优先级队列由批量插队模拟模块填充，内部支持细分优先级编号。
3. 主循环每 1 秒检查一次系统状态：
   - 若模板队列低于 10 个，则启动一批（如 5 个）模板生成任务，结果进入普通模拟队列。
   - 模拟槽位分配遵循 7/1 规则：8 个槽位中 7 个动态槽位优先执行高优先级任务（高优先级队列为空时可执行普通任务），1 个保留槽位仅执行普通任务（普通队列为空时降级执行高优先级任务）。在任一动态槽位上，连续执行高优先级任务数达到阈值（默认 4）后强制切换为执行一个普通任务。
   - 若当日模拟次数接近 5000 次上限，则暂停新任务提交，进入等待状态，但高优先级插队任务仍允许在剩余额度内提交。
4. 对通过筛选的因子，若积攒数量达到阈值（如 20 个），则自动触发一次演化迭代。
5. 实现搜索策略管理：广度优先模式轮询所有感兴趣的区域（如 USA、EUR、CHN），深度优先模式深入有优质因子的区域重点挖掘。

### **3.11 Web Dashboard 可视化模块**

**技术选型**：后端 FastAPI + WebSocket；前端 React + TypeScript。

**实现路径**：

1. 后端实现 RESTful API，提供挖掘任务管理、因子库查询、系统配置读写、批量插队任务管理等接口。
2. 后端建立 WebSocket 连接端点，挖掘协调器在关键事件发生时向所有已连接客户端广播消息。
3. 前端采用 React 构建单页应用，主要页面包括：
   - **总览仪表盘**：展示 LLM 状态、模拟槽位使用情况、今日吞吐量、最近产出因子质量分布等。
   - **批量插队模拟页面**：提供 CSV 文件上传区域、系统内表达式选择器、手动输入区域；任务提交后展示实时进度条、各表达式状态表格；任务完成后提供结果导出按钮。
   - **因子库页面**：支持表格列表查看、按多维度筛选排序、点击查看详细绩效图表和演化世系。
   - **任务控制面板**：提供启动新挖掘任务的表单以及当前运行任务的实时进度条和日志流，同时展示活跃的批量插队任务。
   - **系统配置页**：提供图形化界面修改关键运行参数（如 LLM 模型选择、质量阈值、采样策略）。
4. 前端图表库采用 ECharts 实现因子绩效的时序折线图和分布直方图。

**RESTful API 端点定义**：

**端点**

**方法**

**功能**

`/api/v1/mining/start`

POST

启动新的自动挖掘任务

`/api/v1/mining/{task_id}`

GET

获取指定挖掘任务状态

`/api/v1/mining/{task_id}`

DELETE

取消指定挖掘任务

`/api/v1/batch/upload`

POST

上传 CSV 文件创建批量插队任务

`/api/v1/batch/select`

POST

从因子库选择表达式创建批量任务

`/api/v1/batch/{batch_id}`

GET

获取批量任务进度及结果

`/api/v1/batch/{batch_id}/export`

GET

下载批量任务结果 CSV

`/api/v1/batch/{batch_id}/items/{item_id}/priority`

PATCH

调整未执行任务的优先级

`/api/v1/factors`

GET

查询因子库（支持分页、筛选）

`/api/v1/factors/cache-status`

GET

获取因子缓存状态

`/api/v1/config`

GET/PUT

读取或修改系统配置

`/api/v1/health`

GET

系统健康检查

`/ws/mining/{task_id}`

WebSocket

实时推送进度、日志、指标

## **四、功能测试标准**

各功能模块的测试方法、预期结果和通过条件如下。

### **4.1 平台认证与会话管理模块**

- **测试方法**：提供有效凭证和无效凭证各一组，分别启动会话管理模块；模拟 401 响应和长时间无操作。
- **预期结果**：有效凭证能成功获取会话并完成一次 API 查询；无效凭证应抛出明确的认证异常。遇到 401 后自动重试并恢复调用，不产生应用崩溃。
- **通过条件**：正常认证成功率 100%；自动重认证后后续请求恢复正常的比例不低于 95%。

### **4.2 LLM 服务模块**

- **测试方法**：不断发送请求，并人为中断 Ollama 服务，观测降级行为；使用本地限速工具模拟并发请求。
- **预期结果**：Ollama 可用时本地响应，平均延迟低于 3 秒。降级至 DeepSeek 后功能正常，日志有降级记录。每线程两次调用间隔严格遵守 2 秒限制。
- **通过条件**：本地服务恢复后能自动切回；降级成功率 100%；限流生效后无 429 错误。

### **4.3 模板生成与验证模块**

- **测试方法**：使用包含 10 个不同方向的提示作为输入，收集 100 次生成结果，统计有效表达式比例。
- **预期结果**：至少 80% 的生成结果能够通过语法和语义验证；验证失败的表达式有明确错误分类，并触发自动修正流程，最终有效输出率应提升至 85% 以上。
- **通过条件**：最终有效输出率（含一次修正）≥ 85%，且所有通过验证的表达式均可成功提交模拟（无语法拒绝）。

### **4.4 模拟测试模块**

- **测试方法**：提交 20 个预定义的合法表达式进行模拟，监控并发执行情况和结果返回时间。
- **预期结果**：所有表达式均能正常提交并获取进度 URL，状态轮询能正确捕获“完成”和“失败”状态。提取的夏普、适应度等数据与网页端手动查询一致。整体完成时间不超过 8 分钟。
- **通过条件**：100% 的提交成功率；指标数据一致性无差异；平均单任务处理时间 < 40 秒。

### **4.5 批量插队模拟模块**

- **测试方法**：
  1. 准备一个包含 30 条有效表达式和 5 条故意包含错误（如非法字段）的 CSV 文件，上传并提交。
  2. 从系统因子库选取 20 条表达式提交插队。
  3. 在模拟槽位占用 6 个时提交一批高优先级（含细分优先级：部分为“紧急”，部分为“普通”）任务，观察执行顺序。
  4. 检查任务详情页的进度更新和状态反馈，完成后执行导出。
- **预期结果**：
  - CSV 文件被正确解析，5 条错误表达式在上传后被拦截并显示具体错误原因；其余 30 条成功加入高优先级队列。
  - 从因子库选择的表达式全部成功入队。
  - 高优先级任务在槽位释放后优先于普通队列执行，且不影响正在运行的 6 个任务。优先级高的“紧急”任务先于“普通”高优任务执行。在任一动态槽位上，连续执行高优先级任务达到 4 个后强制切换执行一个普通任务。保留槽位持续执行普通任务。
  - 进度栏实时更新，各表达式状态准确；完成后导出的 CSV 包含全部表达式及结果字段，数据准确。
- **通过条件**：错误拦截率 100%；高优先级任务优先执行顺序符合设计；进度展示延迟 ≤ 2 秒；导出文件内容与实际结果一致，格式可被 Excel 正确打开。

### **4.6 因子筛选与自相关检测模块**

- **测试方法**：构建一组包含高相关性对的 50 个因子，设定夏普阈值 1.25，相似度阈值 0.85。
- **预期结果**：输出因子数量显著少于输入，且任意两个输出因子相似度不超过 0.85。所有输出因子的夏普比率均不低于 1.25。
- **通过条件**：去重率达 30% 以上，且无误杀。

### **4.7 演化优化模块**

- **测试方法**：运行一个初始平均夏普 1.3 的种群 20 代，观察适应度变化。
- **预期结果**：平均适应度和最优适应度呈上升趋势，最终最优夏普超过初始最大值。参数自适应在表现停滞时能增大变异率。
- **通过条件**：最终种群最优适应度提升 ≥ 5%；演化过程无崩溃。

### **4.8 质量监控与退化检测模块**

- **测试方法**：模拟一个因子绩效历史数据，前半段夏普稳定在 1.5，后半段骤降至 0.8。
- **预期结果**：系统在降幅达到 20% 时发出退化告警，健康评分低于 0.5。
- **通过条件**：告警触发准确率 100%，无误报健康因子。

### **4.9 持久化存储模块**

- **测试方法**：运行完整流程产生不少于 500 条模拟记录，执行各种查询。模拟数据库文件损坏后从备份恢复。
- **预期结果**：所有数据完整可查无丢失；查询响应在 1 秒内完成；备份文件经过验证能够恢复全部数据。
- **通过条件**：数据完整性 100%；备份恢复后数据无差异。

### **4.10 挖掘协调调度模块**

- **测试方法**：配置每日限额 50，连续运行至达到限额后继续运行 2 小时；手动重启后读取先前状态。
- **预期结果**：达到限额后立即暂停新模拟提交，日志提示等待，次日 0 点后自动恢复。暂停期间不影响已提交任务的监控和结果回收。重启后能加载已有持久化数据并继续上次进度。
- **通过条件**：限额控制精准（误差 ±2 次）；暂停和恢复期间无异常崩溃；状态恢复成功。

### **4.11 Web Dashboard 可视化模块**

- **测试方法**：打开 Dashboard，启动一个挖掘任务，同时发起一个批量插队任务，观察实时日志、进度百分比、因子数量等面板指标的刷新情况。
- **预期结果**：任务启动后，进度条随模拟完成数实时增长；日志面板滚动显示最新日志；因子列表定时刷新；批量插队任务页面独立显示进度和状态，不与常规挖掘任务混淆。
- **通过条件**：进度刷新延迟 ≤ 2 秒；所有核心指标与实际系统内部状态一致；界面无明显卡顿。

## **五、产品验收标准**

### **5.1 功能完整性**

- 能够使用有效凭证登录 WorldQuant Brain 平台，并正常获取操作符、数据字段等基础信息。
- 支持在无人工干预的情况下，连续自动完成从研究方向提示到生成表达式、验证、提交模拟、筛选、去重、存入因子库的完整闭环，运行周期不低于 72 小时。
- 实现遗传演化优化流程，至少能够执行 5 代以上的正向优化（平均适应度提升）。
- 批量插队模拟功能完整可用：支持 CSV 上传和系统内选择两种提交方式，能正确验证表达式（不修正）、优先执行、实时展示进度、反馈状态、导出结果，且不影响常规自动任务的稳定运行。
- 提供可用的 Web 管理界面，支持挖掘启停、因子库查看、系统配置修改、批量插队任务管理等基本交互。
- 具备完善的数据持久化能力，断电或重启后所有历史数据和运行状态可恢复。

### **5.2 性能指标**

- **生成吞吐量**：在标准硬件配置（8 核 CPU、16GB 内存、NVIDIA GPU 8GB 显存）上，单日可稳定生成并完成模拟的因子数量不低于 100 个（在 1000 次/天限额下）。
- **模拟并发**：支持 8 路并发模拟测试，无资源竞争死锁。
- **响应延迟**：
  - 因子生成（含 LLM 调用）：本地模型平均响应时间 ≤ 5 秒，API 降级模式下 ≤ 20 秒。
  - Web 界面数据查询：首次加载 ≤ 3 秒，后续刷新 ≤ 1 秒。
  - 批量插队 CSV 解析与验证：上传包含 100 条表达式的文件后，5 秒内完成本地解析和校验（不含网络提交时间）。
- **系统稳定性**：7×24 小时连续运行期间，非外部网络原因导致的系统崩溃次数为零。

### **5.3 用户体验要求**

- 安装部署简洁，通过 Docker Compose 一键完成所有依赖服务的拉起。
- 前端界面风格统一、操作逻辑清晰，关键操作（启动挖掘、查看因子、发起批量插队）点击次数不超过 3 次。
- 所有错误信息在界面上有明确的中文提示，并引导用户解决问题（如“凭证无效”、“Ollama 服务未启动”、“CSV 格式有误”）。
- 批量插队任务提供独立且直观的管理视图，进度清晰，操作按钮（提交、取消、导出）易于发现和使用。
- 提供系统运行的健康报告页面，直观展示 CPU/GPU 占用、内存使用、磁盘空间等。

### **5.4 交付物清单**

**序号**

**交付物**

**说明**

1

源代码包

完整的 Python 后端和前端源代码，包含配置文件模板、Docker 编排文件和批量插队功能的全部前后端代码

2

可执行容器镜像

预构建的 Docker 镜像，可直接用于生产部署

3

部署运维手册

详细的安装步骤、环境依赖、配置说明、启动与停止命令，附批量插队功能的配置项说明

4

用户操作手册

面向最终用户的功能使用指南，包含批量插队操作章节，附 CSV 模板格式说明（必需列：expression；可选列：region, universe, delay）

5

接口文档

RESTful API 和 WebSocket 协议说明，包含批量插队相关的所有接口详细定义

6

数据库表结构文档

所有数据库表的字段定义和关系说明，包含批量任务与任务项表

7

测试报告

包含功能测试、性能测试和稳定性测试的结果与结论，覆盖批量插队模块的所有测试用例

8

样例数据集

用于快速验证系统的示例凭证、模拟数据和批量插队 CSV 样例文件

## **附录**

### **A. 术语及缩写解释**

**术语**

**解释**

Alpha 因子

能够预测资产未来收益的可量化特征，通常以数学表达式表示

FASTEXPR

WorldQuant Brain 平台支持的表达式语言

夏普比率 (Sharpe)

风险调整后收益指标，衡量单位风险带来的超额回报

演化算法

模拟生物进化过程的搜索算法，通过选择、交叉、变异迭代改进解的质量

自相关去重

检测并移除与已有因子高度相关的重复候选因子

模拟槽位

系统并发执行模拟测试的逻辑通道，最大数量为 8

插队模拟

用户主动将一批表达式以较高优先级插入模拟队列的行为

Ollama

开源的本地大语言模型运行框架

wqb

WorldQuant Brain Python SDK，封装平台常用 API

### **B. 参考资源**

- WorldQuant Brain 官方文档
- wqb 项目仓库 (`https://github.com/rocky-d/wqb`)
- 《QuantaAlpha、worldquant-miner源码学习》教材

